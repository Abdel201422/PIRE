{% extends 'base.html.twig' %}

{% block title %}Valoracion{% endblock %}

{% block body %}
    <h1>Valoracion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ valoracion.id }}</td>
            </tr>
            <tr>
                <th>Puntuacion</th>
                <td>{{ valoracion.puntuacion }}</td>
            </tr>
            <tr>
                <th>Comentario</th>
                <td>{{ valoracion.comentario }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_valoracion_index') }}">back to list</a>

    <a href="{{ path('app_valoracion_edit', {'id': valoracion.id}) }}">edit</a>

    {{ include('valoracion/_delete_form.html.twig') }}
{% endblock %}
