<form method="post" action="{{ path('app_valoracion_delete', {'id': valoracion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ valoracion.id) }}">
    <button class="btn">Delete</button>
</form>
