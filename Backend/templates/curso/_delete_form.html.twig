<form method="post" action="{{ path('app_curso_delete', {'cod_curso': curso.cod_curso}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ curso.cod_curso) }}">
    <button class="btn">Delete</button>
</form>
