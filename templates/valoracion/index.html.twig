{% extends 'base.html.twig' %}

{% block title %}Valoracion index{% endblock %}

{% block body %}
    <h1>Valoracion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Puntuacion</th>
                <th>Comentario</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for valoracion in valoracions %}
            <tr>
                <td>{{ valoracion.id }}</td>
                <td>{{ valoracion.puntuacion }}</td>
                <td>{{ valoracion.comentario }}</td>
                <td>
                    <a href="{{ path('app_valoracion_show', {'id': valoracion.id}) }}">show</a>
                    <a href="{{ path('app_valoracion_edit', {'id': valoracion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_valoracion_new') }}">Create new</a>
{% endblock %}
